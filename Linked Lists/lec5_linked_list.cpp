// Reduce time complexity from the previous case.
// Creating a dynamically linked list : takes : alot of time. 
// Takes O(n^2). We need to bring it down to O(n) : Linear time. 
// The traversal of head to the end costs another n - since we are already
// expanding the list to input of size n. 
// Can we overcome the need for the traversal? Yes - we must introduce
// another pointer called tail pointer. 

#include <iostream>
using namespace std;

#include "lec2_node.cpp"

// define new node creation function. [update it to reduce the time complexity] 
Node  


// define the print function. 
int main () {



}